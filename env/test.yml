
docker:
  path: ../test./Dockerfile

ecs:
  repo_name: pss-dev

build_options:


# build-time environment
build_env:

deploy_options:


# runtime env, saved into ecs task
task_env:

# runtime secret ARN
task_secret:
