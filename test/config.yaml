stages:
  test:
    env_files: ['secrets.env']
    ecs_deploy:
      # relative to PWD
      DOCKER_PATH: ./Dockerfile

    ecs_env:
      test_fixed_env: 'test1'
    ecs_secrets:
      test_secret: 'arn::::'
